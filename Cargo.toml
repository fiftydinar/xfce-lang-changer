[package]
name = "xfce-aero-lang-changer"
version = "1.0.0"
edition = "2021"

[features]
default = ["bundled"]
bundled = ["fltk/fltk-bundled"]

[dependencies]
fltk = { version = "1" }
fltk-theme = "0.7"
isolang = { version = "2", features = ["local_names"] }
rust_iso3166 = "0.1"
